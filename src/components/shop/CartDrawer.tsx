import React from 'react';
// This component is deprecated as the site has pivoted to a Lead Generation model.
export function CartDrawer({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) {
  return null;
}